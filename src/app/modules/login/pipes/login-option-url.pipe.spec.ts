import { LoginOptionUrlPipe } from './login-option-url.pipe';

describe('LoginOptionUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new LoginOptionUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
